# Build a variable-name -\> description lookup from cache or user input

Build a variable-name -\> description lookup from cache or user input

## Usage

``` r
.nhanes_build_desc_map(descriptions)
```
