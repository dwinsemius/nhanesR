% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhis_mortality.R
\name{nhis_mortality_download}
\alias{nhis_mortality_download}
\title{Download NHIS Public-Use Linked Mortality Files}
\usage{
nhis_mortality_download(
  years = NULL,
  refresh = FALSE,
  quiet = !getOption("nhanesR.verbose", TRUE)
)
}
\arguments{
\item{years}{Character or numeric vector of NHIS survey years (e.g.
\code{"1997"} or \code{1997}). Defaults to all years with a public-use LMF. See
\code{\link[=nhis_lmf_years]{nhis_lmf_years()}}.}

\item{refresh}{Logical. Re-download even if a cached file exists? Default
\code{FALSE}.}

\item{quiet}{Logical. Suppress download messages? Default uses the
\code{nhanesR.verbose} option.}
}
\value{
Invisibly, a named character vector of local file paths (one per
year). The primary side-effect is writing files to the cache directory
under \verb{mortality/dat/}.
}
\description{
Downloads the fixed-width \code{.dat} mortality files from the CDC FTP server
for one or more NHIS survey years. Files are cached locally; re-downloading
is skipped unless \code{refresh = TRUE}.
}
\details{
NCHS distributes NHIS mortality linkage as one file per \strong{survey year}
(unlike NHANES's one file per 2-year cycle). The public-use LMF provides
mortality follow-up through \strong{December 31, 2019} for NHIS 1986-2018.
}
\examples{
\donttest{
# Download specific years (downloading all 33 available years at once,
# via nhis_mortality_download() with no arguments, works the same way but
# is a much larger multi-file FTP batch -- prefer a specific year vector
# for routine use)
nhis_mortality_download(c("2015", "2016", "2017"))
}
}
\seealso{
\code{\link[=nhis_mortality_parse]{nhis_mortality_parse()}}, \code{\link[=nhis_mortality_link]{nhis_mortality_link()}},
\code{\link[=nhanes_mortality_download]{nhanes_mortality_download()}} for the NHANES equivalent.
}
