% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhis_data.R
\name{nhis_download}
\alias{nhis_download}
\title{Download and parse an NHIS Household or Person file}
\usage{
nhis_download(module, years = NULL, refresh = FALSE)
}
\arguments{
\item{module}{Character. \code{"household"} or \code{"person"}.}

\item{years}{Character or numeric vector of NHIS survey years. Defaults
to all years available for \code{module} (see \code{\link[=nhis_data_years]{nhis_data_years()}}).}

\item{refresh}{Logical. Re-download/re-parse even if a cached RDS exists?
Default \code{FALSE}.}
}
\value{
If a single year is requested, a data frame. If multiple years
are requested, a named list of data frames keyed by year. Every
returned data frame carries a \code{.nhis_data_year} integer column (the
requested survey year, not to be confused with a native \code{YEAR}/
\code{SRVY_YR} column the file itself may already contain).
}
\description{
Downloads the fixed-width \code{.zip}, unzips it, downloads the matching SAS
input-syntax file NCHS publishes alongside it, and parses both together
using \code{\link[SAScii:read.SAScii]{SAScii::read.SAScii()}} -- reusing NCHS's own column-position
specification rather than a hand-built one. Results are cached locally as
RDS files, same convention as \code{\link[=nhanes_download]{nhanes_download()}} and
\code{\link[=nhis_mortality_parse]{nhis_mortality_parse()}}.
}
\section{Column names differ by era}{

1997 onward uses NCHS's own descriptive variable names directly
(\code{SRVY_YR}, \code{HHX}, \code{WTFA_HH}, ...), taken straight from that era's SAS
syntax file. \strong{1986-1996 uses NCHS's original placeholder scheme}
instead (e.g. \code{HH_22}, \code{PX_24}) -- that's literally what those years' SAS
syntax files contain. Column NAMES are never changed (a placeholder stays
a placeholder), but for \strong{1986-1991} each placeholder column gets a
\code{"label"} attribute (haven/labelled convention, same as every other
labelled column in this package) with its real meaning, built from that
year's own \code{NHISCORE.PDF} codebook (\code{data-raw/build_nhis_pre1997_crosswalk.R}).
Check with \code{attr(df$HH_22, "label")}, or \code{\link[=NH_describe]{NH_describe()}} to see all of
them at once. \strong{1992-1996 do not get labels} -- checked directly, not
just left undone: the source PDF's own page layout changes enough
starting in 1992 (its \code{NHISCORE.PDF} roughly triples in page count that
year) that the crosswalk-building parser's match rate drops hard, and
unlike 1986-1991, a meaningful share of what doesn't match already has a
descriptive native variable name anyway (e.g. \code{REGION}, \code{HEIGHT},
\code{WEIGHT} for 1993), so the gap matters less than the raw match-rate drop
suggests. Extending the crosswalk past 1991 is possible future work, not
done here.
}

\examples{
\donttest{
hh_2015 <- nhis_download("household", "2015")
}
}
\seealso{
\code{\link[=nhis_data_years]{nhis_data_years()}} for years with data available;
\code{\link[=nhis_mortality_link]{nhis_mortality_link()}} to join mortality follow-up onto the result.
}
