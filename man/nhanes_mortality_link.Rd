% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortality.R
\name{nhanes_mortality_link}
\alias{nhanes_mortality_link}
\title{Link mortality data onto an NHANES analytic dataset}
\usage{
nhanes_mortality_link(
  nhanes_data,
  cycles = NULL,
  keep_vars = NULL,
  download = TRUE
)
}
\arguments{
\item{nhanes_data}{A data frame with at minimum a \code{SEQN} column and a
\code{cycle} column (added automatically by \code{\link[=nhanes_download]{nhanes_download()}}). If \code{cycle}
is absent, \code{cycle} must be supplied.}

\item{cycles}{Character vector of cycle labels present in \code{nhanes_data}.
Required if \code{nhanes_data} does not have a \code{cycle} column.}

\item{keep_vars}{Character vector of LMF variables to retain. Defaults to
all: \code{c("ELIGSTAT", "MORTSTAT", "UCOD_LEADING", "DIABETES", "HYPERTEN", "PERMTH_INT", "PERMTH_EXM")}.}

\item{download}{Logical. Download missing LMF files automatically? Default
\code{TRUE}.}
}
\value{
\code{nhanes_data} with LMF columns appended. Rows with no mortality
record (i.e., SEQNs not present in the LMF) will have \code{NA} for all LMF
columns — this should not occur for continuous NHANES 1999-2018.
}
\description{
Performs a left join of the parsed LMF onto a data frame containing NHANES
participants, matched on \code{SEQN}. Automatically handles multiple cycles by
row-binding the appropriate LMF files before joining.
}
\examples{
\dontrun{
demo <- nhanes_download("DEMO", "2015-2016")
demo_mort <- nhanes_mortality_link(demo)
}
}
\seealso{
\code{\link[=nhanes_survival_prep]{nhanes_survival_prep()}} to convert the linked data into a survival
dataset; \code{\link[=nhanes_lmf_cycles]{nhanes_lmf_cycles()}} for the cycles with public-use LMF;
\code{\link[=nhanes_stack]{nhanes_stack()}} to row-bind multi-cycle data before linking.
}
