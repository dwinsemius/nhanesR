% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe.R
\name{NH_unlabel}
\alias{NH_unlabel}
\title{Remove NHANES variable labels before modeling}
\usage{
NH_unlabel(x)
}
\arguments{
\item{x}{A vector or data frame.}
}
\value{
\code{x} with \code{label}/\code{labels} attributes removed. For vectors,
labelled classes (\code{"labelled"}, \code{"haven_labelled"}, and
\code{"haven_labelled_spss"}) are also removed; ordinary classes such as
\code{"Date"} and factors are retained.
}
\description{
Removes column \code{label} attributes and labelled-vector classes from a vector
or every column in a data frame. This is useful after using \code{\link{NH_label}}
or receiving data that already carries \code{Hmisc}/\code{haven} labels, once
those labels have served their documentation purpose and the data are headed
into model fitting or \code{predict(newdata=)} construction.
}
\details{
The underlying recycling behavior is in base R, and the preservation of
labels on some scalar summaries is a class-method behavior of packages such
as \code{Hmisc}. \code{nhanesR} provides this helper because NHANES labels are
commonly introduced at the package boundary before downstream modeling code
sees them.
}
\examples{
d <- data.frame(RIDAGEYR = 40:50)
attr(d$RIDAGEYR, "label") <- "Age (years)"

d_model <- NH_unlabel(d)
attr(d_model$RIDAGEYR, "label")
}