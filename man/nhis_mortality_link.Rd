% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhis_mortality.R
\name{nhis_mortality_link}
\alias{nhis_mortality_link}
\title{Link NHIS mortality data onto an NHIS analytic dataset}
\usage{
nhis_mortality_link(
  nhis_data,
  years = NULL,
  keep_vars = NULL,
  download = TRUE,
  publicid_col = "PUBLICID",
  year_col = "SRVY_YR"
)
}
\arguments{
\item{nhis_data}{A data frame containing NHIS participants.}

\item{years}{Character or numeric vector of NHIS survey years present in
\code{nhis_data}. Inferred from \code{year_col} when omitted.}

\item{keep_vars}{Character vector of LMF variables to retain. Defaults to
all: \code{c("ELIGSTAT", "MORTSTAT", "UCOD_LEADING", "DIABETES", "HYPERTEN", "DODQTR", "DODYEAR", "WGT_NEW", "SA_WGT_NEW")}.}

\item{download}{Logical. Download missing LMF files automatically? Default
\code{TRUE}.}

\item{publicid_col}{Character. Name of the participant public-use ID
column in \code{nhis_data}. Default \code{"PUBLICID"} (NCHS standard).}

\item{year_col}{Character. Name of the survey-year column in \code{nhis_data}.
Default \code{"SRVY_YR"} (the NHIS public-use file convention).}
}
\value{
\code{nhis_data} with LMF columns appended. Rows with no mortality
record (PUBLICID/year combinations absent from the LMF) will have \code{NA}
for all LMF columns.
}
\description{
Performs a left join of the parsed NHIS LMF onto a data frame containing
NHIS participants, matched on the participant public-use ID \strong{and} survey
year.
}
\details{
\code{nhis_data} must come from somewhere else -- nhanesR does not download NHIS
survey data (see the package-level scoping note in this file's source, or
\code{?nhis_mortality_download}). Typical sources are NCHS's own NHIS public-use
microdata site or IPUMS Health Surveys (IPUMS NHIS).
}
\section{Why the join requires a year column, unlike \code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}}}{

\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} matches on \code{SEQN} alone, which is safe because
NHANES's \code{SEQN} numbering does not overlap across the 2-year cycles this
package pools. NHIS's \code{PUBLICID} construction is not documented clearly
enough (see NCHS's linkage-methods appendix, referenced in the LMF
codebook) to assume the same global uniqueness across survey years, so
this function matches conservatively on the \verb{(PUBLICID, year)} pair rather
than \code{PUBLICID} alone.
}

\examples{
\donttest{
# nhis_data must be supplied from elsewhere -- nhanesR does not download it
# nhis_data <- your_nhis_loading_function(...)
# nhis_data_mort <- nhis_mortality_link(nhis_data)
}
}
\seealso{
\code{\link[=nhis_lmf_years]{nhis_lmf_years()}} for years with a public-use LMF;
\code{\link[=nhis_mortality_parse]{nhis_mortality_parse()}} which produces the input to this join;
\code{\link[=nhanes_mortality_link]{nhanes_mortality_link()}} for the NHANES equivalent.
}
