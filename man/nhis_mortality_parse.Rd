% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhis_mortality.R
\name{nhis_mortality_parse}
\alias{nhis_mortality_parse}
\title{Parse NHIS Linked Mortality Files into data frames}
\usage{
nhis_mortality_parse(years = NULL, refresh = FALSE, download = TRUE)
}
\arguments{
\item{years}{Character or numeric vector of NHIS survey years. Defaults to
all available. See \code{\link[=nhis_lmf_years]{nhis_lmf_years()}}.}

\item{refresh}{Logical. Re-parse even if a cached RDS exists? Default
\code{FALSE}.}

\item{download}{Logical. Auto-download missing \code{.dat} files? Default
\code{TRUE}.}
}
\value{
A named list of data frames. Each data frame contains:
\describe{
\item{PUBLICID}{NHIS public-use identifier (join key -- character, not
numeric; NHIS does not use NHANES's \code{SEQN}).}
\item{ELIGSTAT}{Eligibility: 1=eligible; 2=under 18; 3=insufficient data.}
\item{MORTSTAT}{Vital status: 0=assumed alive; 1=assumed deceased.}
\item{UCOD_LEADING}{Underlying cause of death (ICD-10 recode).}
\item{DIABETES}{Diabetes mentioned on death certificate (1=yes).}
\item{HYPERTEN}{Hypertension mentioned on death certificate (1=yes).}
\item{DODQTR}{Quarter of death (1-4).}
\item{DODYEAR}{Year of death.}
\item{WGT_NEW}{Person-level sample weight, adjusted for linkage
ineligibility. Blank for 1986 (NCHS recommends \code{WTFA} from the
NHIS public-use file that year instead).}
\item{SA_WGT_NEW}{Sample Adult sample weight, adjusted for linkage
ineligibility. Only populated 1997 onward (the Sample Adult File
did not exist before the 1997 NHIS redesign).}
}
}
\description{
Reads the fixed-width \code{.dat} files (downloading them first if needed) and
returns a named list of data frames, one per survey year.
}
\details{
Variable labels are attached as the \code{"label"} attribute on each column,
following the \code{haven}/\code{labelled} convention -- same as
\code{\link[=nhanes_mortality_parse]{nhanes_mortality_parse()}}.
}
\note{
Unlike the NHANES LMF, \strong{NHIS's public-use LMF has no
\code{PERMTH_INT}/\code{PERMTH_EXM}-equivalent person-months variable.}
Constructing follow-up time requires combining \code{DODQTR}/\code{DODYEAR} with
the participant's own NHIS interview year/quarter, which comes from the
NHIS survey data itself, not this file. There is no \code{nhis_survival_prep()}
analogous to \code{\link[=nhanes_survival_prep]{nhanes_survival_prep()}} for this reason -- see
\code{\link[=nhis_mortality_link]{nhis_mortality_link()}}'s documentation for how far this package takes
NHIS mortality linkage.
}
\examples{
\donttest{
lmf <- nhis_mortality_parse(c("2015", "2016"))
lmf[["2015"]]
}
}
\seealso{
\code{\link[=nhis_mortality_download]{nhis_mortality_download()}} to download the raw \code{.dat} files;
\code{\link[=nhis_mortality_link]{nhis_mortality_link()}} to join parsed mortality data onto NHIS data.
}
